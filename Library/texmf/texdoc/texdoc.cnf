# Personal configuration file for `texdoc`

# Append `tex` to the list of allowed extensions (note: empty string at end)
ext_list = pdf, html, htm, txt, dat, md, ps, dvi, tex,  

# Set Neovim as the default pager for text files (and for `tex` files)
viewer_txt = nvim %s
viewer_tex = nvim %s
